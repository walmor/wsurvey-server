import facebookTokenValidator from './facebook-token-validator';
import googleTokenValidator from './google-token-validator';

const tokenValidators = {
  facebookTokenValidator,
  googleTokenValidator,
};

export default tokenValidators;
